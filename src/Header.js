import './styles.css';

export default function Header() {
    return (
        <div style={{backgroundColor: '#c9adf0', display: 'block'}}>
            <h1 style={{textAlign: 'left'}}>Hello :)</h1>
        </div>
    );
}